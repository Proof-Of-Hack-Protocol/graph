{
  "name": "ProofOfHackProtocol",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ ProofOfHackProtocol",
    "create-local": "graph create --node http://localhost:8020/ ProofOfHackProtocol",
    "remove-local": "graph remove --node http://localhost:8020/ ProofOfHackProtocol",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ProofOfHackProtocol"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.2",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
